package h1;

public class H1_main {
	public static void main(String[] args) {

	    // Startmuster: einige lebendige Zellen
	    /* Cell[] startCells = {
	    	 new Cell(1, 1),
	         new Cell(2, 2),
	         new Cell(2, 3),
	         new Cell(3, 1),
	         new Cell(3, 2)
	    };
	
	    // Grid erzeugen (5x5)
	    Grid grid = new Grid(startCells, 5, 5);
	
	    System.out.println("Generation 0:");
	    printGrid(grid);
	
	    // 5 Generationen durchlaufen
	    for (int gen = 1; gen <= 5; gen++) {
	        grid.computeNextGen(); // oder computeGeneration(1) für eine Iteration
	        System.out.println("\nGeneration " + gen + ":");
	        printGrid(grid);
	    } */
	}
}
