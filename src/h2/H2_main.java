package h2;

public class H2_main {
	public static void main(String[] args) {
		/*    	Spielbrett brett = new Spielbrett(5);

		        char[][] dirs = {
		            {'U','R','L','L','D'},
		            {'L','R','L','L','D'},
		            {'U','L','L','L','R'},
		            {'D','R','U','L','L'},
		            {'U','R','L','D','D'}
		        };

		        // Felder setzen (alle boese = false)
		        for (int i = 0; i < 5; i++) {
		            for (int j = 0; j < 5; j++) {
		                brett.getBrett()[i][j] = new Feld(false, dirs[i][j]);
		            }
		        }

		        // Spielstein auf (3,2)
		        Spielstein stein = new Spielstein(brett, 3, 2);

		        stein.go(3);

		        System.out.println("Endposition:");
		        System.out.println("Row: " + stein.getCurrentRow());
		    	System.out.println("Col: " + stein.getCurrentCol()); */
		    }
}
